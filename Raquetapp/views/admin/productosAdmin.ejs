<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    OPENsourceTENNIS
  </title>
  <link rel="icon" type="image/x-icon" href="/favicon2.png">
  <link rel='stylesheet' href='/css/modal.css' />
  <link rel='stylesheet' href='/css/style.css' />
  <link rel='stylesheet' href='/css/productos.css' />
  <link rel='stylesheet' href='/css/style.css' />
  <link rel='stylesheet' href='/css/partials/1header.css' />
  <link rel='stylesheet' href='/css/partials/2footer.css' />
</head>

<body>
  <!-- Header: es estático. Solo muestra logo y titulo -->
  <%- include('../partials/1header.ejs') %>

    <main>
      <% if (typeof msj !=="undefined" && msj) { %>
        <h3>
          <%= msj %>
        </h3>
        <% } %>
          <div id="items_grid">
            <% if (typeof producto !=="undefined" && producto !==null ) { %>
              <% producto.forEach((p)=> { %>
                <div class="navs nav_raquetas">
                  <p class="nombre">
                    <%= p.nombre %>
                  </p>
                  <% if (!p.activo) { %>
                    <p>INACTIVO</p>
                    <button type="button" class="btn_activar" data-id="<%= p.id %>">Activar</button>
                    <div class="modal_confirmacion displayNone" id="modal_<%= p.id %>">
                      <p>Confirmar activación del producto <%= p.nombre %>?</p>
                      <button type="button" class="btn_confirmar_modal" data-id="<%= p.id %>">Confirmar</button>
                      <button type="button" class="btn_cerrar_modal" data-id="<%= p.id %>">Cancelar</button>
                    </div>
                    <% } %>
                      <p class="descripcion">
                        <%= p.descripcion %>
                      </p>
                      <a href="<%=tipo%>/<%= p.id%>">
                        <img class="nav_img_raquetas" src="<%= p.imagen %>" alt="<%= p.nombre %>">
                      </a>
                      <p class="precio">$<%= p.precio %>
                      </p>

                </div>
                <% }) %>
                  <% } %>
          </div>
          <div id="paginacion" class="paginacion">
            <button id="btn_anterior">Anterior</button>
            <button id="btn_siguiente">Siguiente</button>
          </div>
    </main>

    <!-- Footer: es estático. Solo muestra logo y titulo / y en Desk muestra links a redes etc -->
    <%- include('../partials/2footer_admin.ejs') %>


      <script src="/js/admin/productoActivar.js"></script>
      <script src="/js/item.js"></script>
      <script src="/js/productos.js"></script>
      <script src="/js/footer.js"></script>
      <script src="/js/btn_pagar.js"></script>

</body>

</html>